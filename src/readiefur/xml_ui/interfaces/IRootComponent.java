package readiefur.xml_ui.interfaces;

/**
 * Indicates that the class is a root UI component and cannot be a child of another component.
 */
//I use an interface for this rather than an attribute because it allows me to use generic restraints.
public interface IRootComponent {}
